<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rouyi.flow.repo.dao.ActExpandBusinessDao">

    <resultMap type="com.rouyi.flow.domain.dto.ActBusinessTypeDto" id="actExpandBusinessDto">
        <id     property="id"      column="id"      />
        <result property="code"    column="business_code"    />
        <result property="name"     column="business_name"     />
        <result property="status"   column="status"   />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <select id="queryBusinessType" resultMap="actExpandBusinessDto">
        select business_code, business_name, create_time, update_time, status from t_act_expand_business
        <where>
            <if test="status != null" >
                status = #{status}
            </if>
        </where>
    </select>
</mapper> 